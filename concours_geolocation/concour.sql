create table places (
    id integer primary key autoincrement,
    service text,
    url text,
    number text,
    street text,
    city text,
    postal_code text,
    province text,
    lat text,
    lng text,
    category text
);